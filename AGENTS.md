# AGENTS.md

## 프로젝트 목적

스프린트 미션 5 제출용으로 옥토퍼스 토픽의 핵심 가치를 홍보하는 정적 랜딩페이지를 제작한다.

## 기술 제한

- HTML5, CSS3, Vanilla JavaScript만 사용한다.
- React, Vue, Next.js, Tailwind, Bootstrap, jQuery를 사용하지 않는다.
- 별도 빌드 과정이 없어야 한다.
- 루트의 `index.html`, `styles.css`, `script.js`를 사용한다.
- Vercel에서 정적 사이트로 바로 배포할 수 있어야 한다.

## 필수 문서

작업 전에 다음 파일을 순서대로 읽는다.

1. `docs/01_PROJECT_BRIEF.md`
2. `docs/02_LANDING_COPY.md`
3. `docs/03_WIREFRAME.md`
4. `docs/04_DESIGN_SYSTEM.md`
5. `docs/05_INTERACTIONS.md`
6. `docs/06_ACCEPTANCE_CRITERIA.md`

## 콘텐츠 원칙

- 서비스의 핵심은 TOPIK 학습과 내부 포인트를 연결하는 것이다.
- 포인트는 매일의 학습과 54번 정밀 첨삭을 연결하는 성장 보상이다.
- 54번 정밀 첨삭을 받는 학습 혜택으로 사용한다.
- 공식 TOPIK 기관과 제휴한 것처럼 표현하지 않는다.
- 예상 점수는 공식 성적이 아닌 학습용 분석이라고 표현한다.
- 아직 제공하지 않는 기능을 이미 상용화된 것처럼 과장하지 않는다.

## 디자인 원칙

- 모바일 우선 반응형으로 제작한다.
- 기존 옥토퍼스 토픽의 보라색 브랜드를 유지한다.
- 배경은 밝고 깨끗하게, CTA는 선명하게 표현한다.
- 카드, 버튼, 배지의 라운드와 여백을 일관되게 사용한다.
- 과도한 그라데이션, 그림자, 애니메이션을 피한다.
- 외국인 한국어 학습자도 쉽게 이해할 수 있는 짧은 문장을 사용한다.

## HTML 원칙

- `header`, `nav`, `main`, `section`, `footer`를 사용한다.
- 제목 계층은 `h1 → h2 → h3` 순서로 유지한다.
- 버튼 역할에는 `button`, 링크 이동에는 `a`를 사용한다.
- 모든 이미지에 의미 있는 `alt`를 작성한다.
- 폼 input에는 label을 연결한다.

## CSS 원칙

- CSS 변수로 색상, 여백, 라운드 값을 관리한다.
- Flexbox 또는 Grid로 레이아웃을 구성한다.
- 최소 360px부터 정상 작동해야 한다.
- 본문 글자 크기는 모바일에서 16px 이상을 기본으로 한다.
- 포커스 표시를 제거하지 않는다.

## JavaScript 원칙

다음 기능만 구현한다.

- 모바일 내비게이션 열기/닫기
- 섹션 부드러운 이동
- FAQ 아코디언
- 체험 신청 폼 검증 및 성공 메시지
- 현재 연도 자동 표시
- 스크롤 등장 효과는 `prefers-reduced-motion`을 존중한다.

사용자 이메일을 외부 서버로 전송하지 않는다. 무료 체험 폼의 입력 내용은 브라우저 `localStorage`에만 저장하고, 이를 화면에 명확히 안내한다.

## 금지 사항

- 현금 지급, 출금, 상품권 교환 문구 추가 금지
- 실제 이용자 수나 점수 상승률을 근거 없이 작성 금지
- 외부 저작권 이미지 무단 사용 금지
- API 키 또는 개인정보 포함 금지
- 불필요한 라이브러리 추가 금지
- 문구를 임의로 크게 변경하지 않는다.

## 완료 전 점검

- HTML 시맨틱 구조
- 모바일 360px
- 태블릿 768px
- 데스크톱 1280px
- CTA 버튼 작동
- 폼 오류·성공 상태
- 키보드 탐색
- 이미지 경로
- Vercel 정적 배포 가능 여부
